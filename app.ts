let revenue: number = 1000;
let bonus: number = 500;
let c: string = 'string';
let d: boolean = false;

let res: number = revenue + bonus;

function getFullName(firstname: string, lastname: string): string {
	return `${firstname} ${lastname}`;
}

const skiils: string[] = ['name', 'firstname', 'lastname'];
